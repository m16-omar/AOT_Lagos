import React from "react";
import PropTypes from "prop-types";

const shapes = {
    round: "rouded-[24px]",
};

const variants = {
    outline: {
        primary: "border-primary border-[0.5px] border-solid text-low_constrast",
    }
}